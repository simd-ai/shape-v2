# Ablation: patch sizes 4, 6, 8
# Override processor.patch_size from CLI

processor:
  patch_size: 4
  hidden_size: 128
  num_layers: 3

tokenizer:
  latent:
    latent_shape: [24, 24, 24]

train:
  epochs: 30
  batch_size: 64
